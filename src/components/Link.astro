---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a
  class={'action highlight' + (className ? ' ' + className : '')}
  href={href}
  target='_blank'
  rel='noopener noreferrer'
>
  <slot />
</a>
